# API URLs - both point to prod to prevent localhost calls in production
PUBLIC_API_URL_PROD=https://api.statox.fr
PUBLIC_API_URL_LOCAL=https://api.statox.fr

PUBLIC_BLOG_URL=https://statox.fr

# This is defined only on local env to disply debug values
PUBLIC_ENVIRONMENT=prod
