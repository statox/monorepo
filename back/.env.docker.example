# Docker Environment Configuration Template
# Copy this file to .env.docker and fill in your actual values
# DO NOT commit .env.docker to git (it's in .gitignore)

# NOTE: When using docker-compose, many of these values are already set
# as environment variables in docker-compose.yml. This file is for
# additional configuration or overriding defaults.

# Database Configuration (defaults from docker-compose.yml)
# DB_HOST=db
# DB_PORT=3306
# DB_NAME=db
# DB_USER=root
# DB_PASSWORD=example

# Session (override the default in docker-compose.yml)
# SESSION_SECRET=your-secret-key-here

# Elasticsearch (configured in docker-compose.yml for local dev)
# ELK_HOST=elk
# ELK_PORT=9200
# Leave empty to disable ELK logging:
# ELK_HOST=
# ELK_PORT=

# S3/R2 Configuration
# For local dev, docker-compose.yml uses LocalStack:
# R2_ENDPOINT=http://localstack:4566
# For production, configure these:
R2_REGION=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=

# Notifications
SLACK_WEBHOOK_URL=
SLACK_USER_ID=
NTFY_SH_TOPIC_URL=

# Meteo France (optional)
METEOFRANCE_TOKEN=

# Application Config
PORT=3000
ENV=production
TZ=UTC
NODE_ENV=production
