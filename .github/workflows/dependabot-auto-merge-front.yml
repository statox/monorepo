# This workflow is used to run the tests when dependabot creates a PR and automatically
# merge the PR.
#
# See https://www.statox.fr/posts/2024/04/github_dependabot_auto_merge/ for companion article
#
# The build is used as State Checks for the PR
# And the steps trigger the approval of the PR and the setup of the auto-merge setting
#
# TODO:
# - Maybe restrict the types for the pull_request event

name: (front) AutoMerge Dependabot PRs

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main]
    paths:
      - 'front/**'

permissions:
  # This is needed to approve the PR
  pull-requests: write
  # This is needed to merge the PR https://github.com/cli/cli/issues/6695#issuecomment-1348430969
  contents: write

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}

      - name: Cache node and npm
        uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: 'npm'
          cache-dependency-path: ./front/package-lock.json

      - name: Cache node modules
        id: cache-nodemodules
        uses: actions/cache@v5
        env:
          cache-name: cache-node-modules
        with:
          # caching node_modules
          path: node_modules
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      # packages/sdk is consumed by front/ as a file: symlink. Vite resolves its
      # TypeScript source directly, which means the SDK's own dependencies (ajv,
      # json-schema-to-ts) must be installed separately - front/'s npm install
      # does not descend into linked packages. A proper npm workspaces setup at
      # the repo root would handle this automatically in the future.
      - name: Install SDK dependencies
        working-directory: packages/sdk
        run: npm ci

      - name: Install Dependencies
        working-directory: front
        if: steps.cache-nodemodules.outputs.cache-hit != 'true'
        run: npm ci

      - name: eslint check
        working-directory: front
        run: npm run lint

      - name: prettier check
        working-directory: front
        run: npm run prettier

      - name: svelte check
        working-directory: front
        run: npm run check

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}

      - name: Cache node and npm
        uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: 'npm'
          cache-dependency-path: ./front/package-lock.json

      - name: Cache node modules
        id: cache-nodemodules
        uses: actions/cache@v5
        env:
          cache-name: cache-node-modules
        with:
          path: node_modules
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      # packages/sdk is consumed by front/ as a file: symlink. Vite resolves its
      # TypeScript source directly, which means the SDK's own dependencies (ajv,
      # json-schema-to-ts) must be installed separately - front/'s npm install
      # does not descend into linked packages. A proper npm workspaces setup at
      # the repo root would handle this automatically in the future.
      - name: Install SDK dependencies
        working-directory: packages/sdk
        run: npm ci

      - name: Install Dependencies
        working-directory: front
        if: steps.cache-nodemodules.outputs.cache-hit != 'true'
        run: npm ci

      - name: build
        working-directory: front
        run: npm run build

  auto-merge-dependabot:
    needs: [checks, build]
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    steps:
      - name: Approve the PR
        run: gh pr review --approve "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}

      - name: Auto merge the PR
        run: gh pr merge --rebase --auto "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
