# Monorepo usage

- In new sessions read README.md and REPO_OVERVIEW.md files to get some context on the project
- Each change in a backend route (added/removed/schema updated/auth updated) must trigger an SDK generation
